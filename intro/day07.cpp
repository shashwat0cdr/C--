#include <iostream>
#include <typeinfo>
using namespace std;
int main()
{
    /*
     // implicit type conversion
     int num1 = 10;
     float num2 = 5.5;
     float sum = num1 + num2;
     cout << "The sum of " << num1 << " and " << num2 << " is: " << sum << endl;
     cout<< "The type of sum is: " << typeid(sum).name() << endl;
   */
    /*
    // char to int conversion
    char ch = 'A';
    char a = ch + 1; // implicit type conversion from char to int
    cout << a << endl;
    */
    /*
    // int to char conversion
    int num = 66;
    char b = num; // implicit type conversion from int to char
    cout << b << endl;
    */
    /*
    // Explicit type conversion
    int num1 = 10;
    float num2 = 5.5;
    float sum = num1 + (int)num2; // explicit type conversion from float to int
    cout << "The sum of " << num1 << " and " << (int)num2 << " is: " << sum << endl;
    */
    /*
    // double to int conversion
    double pie = 3.14159;
    int intpi = (int)pie; // explicit type conversion from double to int
    cout << "The value of pie is: " << pie << endl;
    cout << "The value of intpi is: " << intpi << endl;
    */
    /*
    ## BONUS
     int/int = int
     int/float = float
     float/int = float
    */

    
    return 0;
}