#include <iostream>
using namespace std;

int main()
{

    /*
    int a = 5;
     cout << (++a);
     cout << (a++);
     cout << (a--);
     cout << (--a);
     cout << (a);

 */
    /*
        bool cond1 = (10 > 5);
        bool cond2 = (3 < 6);
        bool cond3 = (3 != 7);
        // And operator
        if (cond1 && cond2 && cond3)
        {
            cout << "All the conditions are true" << endl;
        }
        else
        {
            cout << "All Conditions are not true" << endl;
        }
        // or operator
        if (cond1 || cond2 || cond3)
        {
            cout << "All the conditions are true" << endl;
        }
        else
        {
            cout << "all the conditions are not true" << endl;
        }
        // not operator
        bool condition = (5 != 10);
        cout << condition << endl;
    */
    /*
    int arr = 5;
    cout << "The value of arr is: " << arr << endl;
     arr = arr + 5;
     cout << "The value of arr is: " << arr << endl;
     arr *= 10;
     cout << "The value of arr is: " << arr << endl;
    */
    
    return 0;
}